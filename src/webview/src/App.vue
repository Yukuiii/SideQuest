<script setup lang="ts">
/**
 * 根组件
 * 使用 RouterView 渲染当前路由对应的组件
 */
</script>

<template>
  <RouterView />
</template>

<style>
:root {
  font-family: var(--vscode-font-family, system-ui, sans-serif);
  font-size: var(--vscode-font-size, 13px);
  color: var(--sq-foreground);
  background: var(--sq-background);
}
</style>
